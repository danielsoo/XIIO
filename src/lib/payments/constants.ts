/** Firebase Auth custom claim — uploader deposit completed (spam mitigation, not identity KYC). */
export const DEPOSIT_VERIFIED_CLAIM = "depositVerified";

/** @deprecated Use DEPOSIT_VERIFIED_CLAIM */
export const LEGACY_DEPOSIT_CLAIM = "uploaderDepositVerified";
